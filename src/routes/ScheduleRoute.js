import { Router } from 'express'

const router = Router()

router.get('/schedule', null)
router.get('/schedule/:id', null)
router.put('/schedule/:id', null)
router.delete('/schedule/:id', null)
router.post('/schedule', null)

export default router